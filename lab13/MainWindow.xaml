<Window x:Class="lab13.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab13"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Каталог книг" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <!-- Форма ввода -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Автор:" FontWeight="Bold"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Author, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="0" Grid.Column="2" Text="Название:" FontWeight="Bold"/>
            <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Тираж:" FontWeight="Bold"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Circulation, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="1" Grid.Column="2" Text="Цена:" FontWeight="Bold"/>
            <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Год издания:" FontWeight="Bold"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Year, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15">
            <Button Content="Добавить книгу" Command="{Binding AddBookCommand}" Background="#2ecc71"/>
            <Button Content="Сохранить в бинарный файл" Command="{Binding SaveToBinaryCommand}" Background="#e74c3c"/>
            <Button Content="Загрузить из файла" Command="{Binding LoadFromBinaryCommand}" Background="#9b59b6"/>
            <Button Content="Добавить тестовые данные" Command="{Binding AddSampleDataCommand}" Background="#f39c12"/>
        </StackPanel>

        <!-- Поиск -->
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Ключевое слово для поиска:" FontWeight="Bold"/>
            <TextBox Grid.Column="1" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="2" Content="Найти авторов" Command="{Binding SearchAuthorsCommand}" Background="#34495e"/>
        </Grid>

        <!-- Результаты -->
        <Border Grid.Row="4" BorderBrush="#bdc3c7" BorderThickness="1" CornerRadius="5" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding ResultText}" 
                           TextWrapping="Wrap" 
                           FontFamily="Consolas"
                           Background="#ecf0f1"
                           Padding="10"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
